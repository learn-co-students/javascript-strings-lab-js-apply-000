var greeting = "Hello, everybody!";

var specialGuest = "Hello, Neil deGrasse Tyson!";

var greetSpecialGuest = "Hello, " + specialGuest + "!"; // Using concotenation to create a string for the variable greetSpecialGuest.

var topic = "space";

var conversation = `Let\'s talk about ${topic}.`; // pulling in the variable topic from the global scope and crearint a string through interpolation.
